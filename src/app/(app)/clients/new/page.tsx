import { PlaceholderPage } from "@/components/feedback/placeholder-page";
export default function NewClientPage() { return <PlaceholderPage title="New client" description="Client creation is deferred until the backend mutation DTO is confirmed." />; }
